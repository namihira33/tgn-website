---
import Button from '../ui/Button.astro';

const stats = [
  { label: 'メンバー数', value: '約30名' },
  { label: '開催イベント', value: '100+' },
  { label: '活動年数', value: '13年+' },
  { label: 'メディア掲載', value: '6件' },
];

const highlights = [
  { title: 'つくばQxQ', description: '異分野研究交流イベント' },
  { title: '院生ひろば', description: '院生同士の交流・悩み共有の場' },
  { title: '院生花見', description: '春の恒例・お花見交流会' },
];
---

<section class="section bg-white">
  <div class="container">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <div>
        <h2 class="text-2xl md:text-3xl font-bold text-text mb-4">
          活動実績・成果
        </h2>
        <p class="text-text-muted mb-6">
          TGNは2011年の設立以来、院生プレゼンバトルをはじめとする異分野交流イベントや
          科学コミュニケーション活動を通じて、大学院生同士のネットワーク構築に取り組んできました。
        </p>

        <div class="grid grid-cols-2 gap-4 mb-6">
          {stats.map(stat => (
            <div class="text-center p-4 bg-gray-50 rounded-lg">
              <div class="text-2xl font-bold text-primary">{stat.value}</div>
              <div class="text-xs text-text-muted">{stat.label}</div>
            </div>
          ))}
        </div>

        <div class="flex flex-wrap gap-3">
          <Button href="/achievements" variant="outline">
            業績を詳しく見る
          </Button>
          <a href="/achievements/archive" class="inline-flex items-center gap-2 px-4 py-2 text-sm text-primary hover:text-primary-dark font-medium border border-primary/20 rounded-lg hover:border-primary/40 transition-colors">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
            </svg>
            過去記事
          </a>
          <a href="/achievements/media" class="inline-flex items-center gap-2 px-4 py-2 text-sm text-secondary hover:text-secondary-dark font-medium border border-secondary/20 rounded-lg hover:border-secondary/40 transition-colors">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9.5a2.5 2.5 0 00-2.5-2.5H15" />
            </svg>
            メディア掲載
          </a>
        </div>
      </div>

      <div class="space-y-4">
        {highlights.map((item, index) => (
          <div
            class="flex items-center gap-4 p-4 bg-gradient-to-r from-primary/5 to-transparent rounded-lg fade-in"
            style={`transition-delay: ${index * 100}ms`}
          >
            <div class="w-12 h-12 bg-primary/10 rounded-lg flex items-center justify-center flex-shrink-0">
              <svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
            <div>
              <h3 class="font-bold text-text">{item.title}</h3>
              <p class="text-sm text-text-muted">{item.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
