---
import Button from '../ui/Button.astro';
---

<section class="relative bg-gradient-to-br from-primary to-primary-dark text-white overflow-hidden">
  <!-- Background pattern -->
  <div class="absolute inset-0 opacity-10">
    <svg class="w-full h-full" viewBox="0 0 100 100" preserveAspectRatio="none">
      <defs>
        <pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse">
          <path d="M 10 0 L 0 0 0 10" fill="none" stroke="currentColor" stroke-width="0.5"/>
        </pattern>
      </defs>
      <rect width="100%" height="100%" fill="url(#grid)" />
    </svg>
  </div>

  <div class="container relative">
    <div class="py-20 md:py-32 lg:py-40 grid md:grid-cols-2 gap-8 items-center">
      <!-- スマホ: 画像が先（上）、PC: テキストが左 -->
      <div class="order-2 md:order-1">
        <p class="text-sm md:text-base text-primary-light mb-4 font-medium">つくば院生ネットワーク（TGN）</p>
        <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold leading-tight mb-6">
          大学院生に、<br />
          もう一つのコミュニティを
        </h1>
        <p class="text-lg md:text-xl text-primary-light mb-8 max-w-2xl">
          つくば院生ネットワーク（TGN）は、筑波大学の大学院生による異分野交流・科学コミュニケーション団体です。
          研究室の外で同じ大学院生として悩みや経験を共有できる場を作っています。
        </p>
        <div class="flex flex-wrap gap-4">
          <Button href="/about" variant="secondary" size="lg">
            TGNについて
          </Button>
          <Button href="/about#join" variant="outline" size="lg" class="border-white text-white hover:bg-white hover:text-primary">
            参加する
          </Button>
        </div>
      </div>
      <!-- スマホ: 画像が先（上）、PC: 画像が右 -->
      <div class="order-1 md:order-2">
        <img
          src="/images/hero-members.jpg"
          alt="TGNメンバー"
          class="rounded-2xl shadow-2xl w-full"
        />
      </div>
    </div>
  </div>

  <!-- Wave decoration -->
  <div class="absolute bottom-0 left-0 right-0">
    <svg viewBox="0 0 1440 80" fill="none" preserveAspectRatio="none" class="w-full h-12 md:h-20">
      <path d="M0 80L60 74.7C120 69 240 59 360 53.3C480 48 600 48 720 53.3C840 59 960 69 1080 69.3C1200 69 1320 59 1380 53.3L1440 48V80H1380C1320 80 1200 80 1080 80C960 80 840 80 720 80C600 80 480 80 360 80C240 80 120 80 60 80H0Z" fill="var(--color-background)"/>
    </svg>
  </div>
</section>
