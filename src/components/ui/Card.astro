---
interface Props {
  href?: string;
  class?: string;
}

const { href, class: className = '' } = Astro.props;
const Tag = href ? 'a' : 'article';
---

<Tag
  href={href}
  class:list={['card block', className]}
>
  <slot />
</Tag>
